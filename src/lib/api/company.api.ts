// createCompanyWithOwner fue eliminado del backend. El registro de empresas
// vive ahora en src/graphql/operations/account.ts (createUser → createCompany),
// tipado por codegen. Este archivo queda vacío hasta que se migren aquí otras
// operaciones de compañía (updateCompany, etc.) — o se elimine.
export {};
